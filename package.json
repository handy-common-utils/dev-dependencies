{
  "name": "@handy-common-utils/dev-dependencies",
  "workspaces": [
    "mocha",
    "jest",
    "dummy",
    "common"
  ],
  "devDependencies": {
    "@tsconfig/node20": "^20.1.6",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.3",
    "zx-bulk-release": "^3.2.0"
  },
  "scripts": {
    "test": "npm -w common test && npm -w mocha test && npm -w jest test",
    "format:all": "prettier --write --ignore-unknown .",
    "prepare": "husky"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  },
  "dependencies": {
    "@jest/globals": "^30.3.0",
    "jest": "^30.3.0",
    "ts-jest": "^29.4.9"
  }
}
